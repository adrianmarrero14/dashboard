# Valores por defecto para desarrollo (cargados automáticamente por Next.js).
# En dev, la capa de red apunta al mismo origen `/api`, donde el route handler
# mock de src/app/api actúa de stand-in de dashboard-api (ADR-12).
NEXT_PUBLIC_API_URL=/api
